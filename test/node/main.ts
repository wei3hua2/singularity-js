import * as c from 'chai';
import * as m from 'mocha';

import * as Snet from '../../src/snet';

m.describe.skip('Node environment', () => {
  m.it('should ensure main instance method are working', async function() {
    // c.expect(Snet.create).to.exist;
  });
})
